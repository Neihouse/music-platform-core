// An example of what these will look like

// import { getUser } from "../queries/users";

// export async function userCanDeleteTrack(trackId: string) {
//     const user = await getUser()

//     const track = await getTrackById(trackId)

//     if (track.ownerId (or some shit) === user.data.user?.id) {
//         return true
//     }

//     return false
// }
